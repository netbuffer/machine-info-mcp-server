spring:
  application:
    name: machine-info-mcp-server
  ai:
    mcp:
      server:
        enabled: true
        name: ${spring.application.name}
        version: 1.0.0
        type: async
        stdio: true
  main:
    banner-mode: off
    web-application-type: none
  threads:
    virtual:
      enabled: true

logging:
  level:
    root: OFF